const form=document.getElementById('loginForm');const pwd=document.getElementById('password');document.getElementById('togglePassword').addEventListener('click',()=>{pwd.type=pwd.type==='password'?'text':'password';});form.addEventListener('submit',async e=>{e.preventDefault();const btn=document.getElementById('loginBtn');const box=document.getElementById('loginError');btn.disabled=true;btn.textContent='جاري تسجيل الدخول...';box.innerHTML='';try{await apiRequest('/api/admin/auth/login',{method:'POST',body:JSON.stringify({email:document.getElementById('email').value.trim(),password:pwd.value})});window.location.href='dashboard.html';}catch(err){box.innerHTML='<div class="alert alert-danger">بيانات الدخول غير صحيحة أو تعذر تسجيل الدخول.</div>';}finally{btn.disabled=false;btn.textContent='تسجيل الدخول';}});
